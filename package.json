{
  "name": "practice3",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "start": "browser-sync start --server --files src/**, index.html",
    "sass": "sass --watch src/style.scss:src/style.css",
    "lint": "eslint ./src",
    "lint:watch": "esw --watch",
    "stylelint": "stylelint './src/**/*.css'",
    "dev": "npm run sass | npm run start"
  },
  "author": "Zdebskyy Yaroslav",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.26.0",
    "eslint": "^5.6.1",
    "eslint-watch": "^4.0.2",
    "stylelint": "^9.6.0",
    "stylelint-config-standard": "^18.2.0"
  }
}
